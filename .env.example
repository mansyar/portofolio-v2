# =============================================================================
# Ansyar's Portfolio - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# Required variables are marked with (Required)
# =============================================================================

# =============================================================================
# App Configuration
# =============================================================================
NODE_ENV=development

# Public URL of your deployed app (Required for SEO/OG tags)
VITE_APP_URL=http://localhost:3000

# =============================================================================
# Self-Hosted Convex (Required)
# =============================================================================
# Your self-hosted Convex backend URL (used by client)
VITE_CONVEX_URL=https://convex.your-domain.com

# Convex deployment credentials (used by CLI for schema deployment)
CONVEX_SELF_HOSTED_URL=https://convex.your-domain.com
CONVEX_SELF_HOSTED_ADMIN_KEY=your-admin-key-from-convex-dashboard

# =============================================================================
# Convex Auth (Required for self-hosted)
# =============================================================================
# Your Convex site URL for OIDC discovery
CONVEX_SITE_URL=https://convex.your-domain.com

# JWT signing keys - generate with:
#   openssl genpkey -algorithm RSA -out private.pem -pkeyopt rsa_keygen_bits:2048
#   openssl rsa -in private.pem -pubout -out public.pem
# Then convert to single line with \n for newlines
JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----"

# JWKS (JSON Web Key Set) - convert public key to JWK format
# Use https://russelldavies.github.io/jwk-creator/ or similar tool
JWKS='{"keys":[{"kty":"RSA","n":"...","e":"AQAB","alg":"RS256","use":"sig"}]}'

# =============================================================================
# Admin Authentication (Required)
# =============================================================================
# Email address(es) that have admin access to the CMS
# Comma-separated for multiple admins: admin@example.com,backup@example.com
ADMIN_EMAIL=your-email@example.com

# =============================================================================
# Optional Integrations
# =============================================================================

# Slack Webhook (for contact form notifications)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Resend (for future Magic Link auth support)
# RESEND_API_KEY=re_your-resend-key
